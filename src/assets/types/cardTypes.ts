export interface CardProps {
  imageUrl?: string;
  title?: string;
  paragraph?: string;
  listItem?: string[];
  buttonText?: string;
  icon?: React.ReactNode;
  chat?: string;
  description?: string;
}
